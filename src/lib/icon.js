import { createIconSet } from 'react-native-vector-icons'
const glyphMap = { 
  'search': 59052,
  'play': 58907,
  'pause': 61059,
  'list': 59065
}
const Icon = createIconSet(glyphMap, 'iconfont')

export default Icon